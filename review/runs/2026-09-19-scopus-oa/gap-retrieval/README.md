# Gap-fill retrieval — 2026-09-19-scopus-oa

Targeted extra papers for interpretation gaps listed in `../synthesis-rationale.md` (f)–(g).

| File | Role |
|---|---|
| `search-g*.json` | OpenAlex API hits (not a reference list) |
| `selection-log.json` | Why DOIs were selected or skipped |
| `catalog.json` | The 8 DOIs actually sent to OA fetch |
| `fetch-log.json` | Sought / found / not retrieved (do not overwrite `../fetch-log.json`) |

PDFs (gitignored) are in `papers/scopus-oa-gapfill/`. Extracted notes are in `../notes/`.
